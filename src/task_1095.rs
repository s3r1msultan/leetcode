   struct MountainArray;
   impl MountainArray {
      fn get(&self, index:i32)->i32 {
          0
      }
      fn length(&self)->i32 {
          0
      }
  }


    pub fn find_in_mountain_array(target: i32, mountainArr: &MountainArray) -> i32 {
        let mut start = 0;
        let mut end = mountainArr.length();
        
        
        -1
    }
